import { Request, Response, NextFunction } from "express";


export const createAvailability = (req: Request, res: Response, next: NextFunction) => {
    try {

    } catch (error) {

    }
}

export const getAvailabilitiesByLinkId = (req: Request, res: Response, next: NextFunction) => {
    try {

    } catch (error) {

    }
}